name: CI
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: echo "Run!"
      - name: Build AUR Package
        uses: DuckSoft/build-aur-action@v1.1
        with:
          repo-name: hello
      - name: LS
        run: ls -al *
